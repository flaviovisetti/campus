<div>
  <div>
    <h1><%= @company.name %></h1>
    <%= @company.location%>
    <%= @company.mail%>
    <%= @company.phone%>
  </div>
  <div>
    <%unless @company.jobs.empty?%>
      <%= render @company.jobs %>
    <% else %>
      Nenhuma vaga disponível
    <% end %>
  </div>
</div>

<%= link_to "Voltar", root_path%>
