<div>
  <div>
    <h1><%= @category.name %></h1>
  </div>
  <div>
    <% unless @category.jobs.empty? %>
      <%= render @category.jobs %>
      <% else %>
        Nenhuma vaga disponível
    <% end %>
  </div>
</div>
<%= link_to "Voltar", root_path%>
