<%= form_for @category do |f_category| %>
<%= f_category.label :name, 'Nome' %>
<%= f_category.text_field :name %>
<%= f_category.submit 'Atualizar' %>

<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, class: name %>
<% end %>

<% end %>
